/**
 * Returns a function that once invoked returns a randomly selected value from given `choices`.
 *
 * @param {...*} choices - choices to be returned at random
 * @returns {function} a function that returns a single value from choices
 *
 * @example
 * ```
 * const petto = choose("doggo", "catto", "ratto")
 * petto()  // -> "ratto"
 * petto()  // -> "doggo"
 * petto()  // -> "doggo"
 * petto()  // -> "catto"
 * ```
 */
const choose = (...choices) => {

}


module.exports = choose